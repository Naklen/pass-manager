<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=netstandard">
    <Style Selector="Window">
        <Setter Property="Background" Value="#1b3352"/>
    </Style>
    <Style Selector="ListBox">
        <Setter Property="Background" Value="Transparent"/>
    </Style>    
	<Style Selector="ListBoxItem">
		<Setter Property="Padding" Value="0"/>					
	</Style>
	<Style Selector="ListBoxItem:selected > ContentPresenter">
		<Setter Property="Background" Value="Transparent"/>
	</Style>
	<Style Selector="ListBoxItem:pointerover > ContentPresenter">
		<Setter Property="Background" Value="Transparent"/>					
	</Style>
    <Style Selector="Button">
        <Setter Property="Foreground" Value="LightGray"/>
        <Setter Property="Background" Value="Transparent"/>
    </Style>
	<!--Style Selector="Button /template/ ContentPresenter Svg">
		<Setter Property="Stroke" Value="LightGray"/>
		<Setter Property="Background" Value="Transparent"/>
	</Style-->
    <Style Selector="TextBlock">
        <Setter Property="Foreground" Value="LightGray"/>
    </Style>
	<Style Selector="StackPanel.pass_entry__header">
		<Setter Property="Height" Value="20"/>
	</Style>
</Styles>